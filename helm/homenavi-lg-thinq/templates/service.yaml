apiVersion: v1
kind: Service
metadata:
  name: {{ include "lg-thinq.fullname" . }}
  labels:
    {{- include "lg-thinq.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "lg-thinq.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
